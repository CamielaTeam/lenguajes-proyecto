import {asd, bfs} from 'jiji';
import Componente2 from 'jiji';
import Otro from 'otro';
function Componente(props){
    return(
        <h1 primeraProp={minumero}>
          <Componente2 primeraProp={miNumero}>
          </Componente2>
        </h1>
    );
};
Componente.propTypes = {
  name: PropTypes.number,
  otro : PropTypes.func
};